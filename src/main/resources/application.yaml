spring:
  application:
    name: orderflow
  banner:
    location: classpath:banner.txt

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration